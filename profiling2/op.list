   add_n,
   batch_matmul,
bias_optimizer,
bias_out,
   broadcast_add,
   broadcast_div,
   broadcast_like,
   broadcast_matmul,
   broadcast_mul,
   ? cast,
   ? cast_f2h,
   ? cast_h2f,
   dropout,
   # expand,
   # expand_dims,
   fused_bias_add_gelu,
fused_bias_add_mask_scale,
fused_scale_mask_softmax_dropout,
   # gather, ? dim_gather
   gelu,
   # identity,
   layer_norm,
   log_softmax,
   matmul,
   # narrow,
   nll,
   ones_like,
random_mask_like,
reduce_sum,
reduce_sum_like,
   # reshape,
   scalar_add,
   scalar_logical_greater_equal,
   scalar_mul,
   slice, c

,
   tanh,
   # transpose,
unsorted_segment_sum_like,
weight_optimizer,
weight_out,
weightadam_bias_correction_factor1,
weightadam_bias_correction_factor2